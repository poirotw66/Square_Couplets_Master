# Changelog

## [1.0.12] - 2026-01-20

### 🎉 重大改進：開箱即用

這個版本解決了所有需要手動配置的問題，用戶現在可以直接使用，無需任何調試！

### ✅ 已修復的問題

1. **TypeScript 執行問題**
   - 之前：發布的包包含 `.ts` 文件，Node.js 無法直接執行
   - 現在：自動編譯為 JavaScript，包含在 `dist/` 目錄

2. **依賴缺失問題**
   - 之前：`@google/genai` 在 `devDependencies`，全局安裝時不會安裝
   - 現在：移到 `dependencies`，確保所有依賴都會被安裝

3. **路徑查找問題**
   - 之前：無法找到 `services` 目錄
   - 現在：優先查找編譯後的 `dist/services/`，支持全局和本地安裝

4. **臨時腳本創建問題**
   - 之前：AI 會創建臨時 `.js` 腳本文件
   - 現在：更新 `.cursor/commands/doufang.md`，明確指示直接執行 CLI 命令

### 📦 技術改進

- 創建 `tsconfig.cli.json` 用於編譯 CLI 相關代碼
- 添加 `fix-imports.js` 自動為編譯後的文件添加 `.js` 擴展名
- 創建 `utils/imageUtils.node.ts` 避免瀏覽器 API 依賴
- 更新所有 skill 腳本的路徑查找邏輯
- 添加 `DEBUG_DOUFANG=1` 環境變量用於調試

### 🚀 用戶體驗

**之前的使用體驗：**
```bash
$ doufang-prompt "車水馬龍"
❌ Error: Cannot import TypeScript service module
   Or if you need to use the script directly:
   1. Install tsx: npm install -g tsx
   2. Run: tsx skills/generate-doufang-prompt/index.js "車水馬龍"
```

**現在的使用體驗：**
```bash
$ npm install -g @justin_666/square-couplets-master-skills
$ doufang-prompt "車水馬龍"
✅ 直接生成 prompt，開箱即用！
```

### 📝 新功能

- 添加 `--version` 支持所有 CLI 命令
- 優化錯誤提示信息
- 支持從任意目錄執行命令

### 🔧 開發改進

- `npm run build:cli` - 編譯 TypeScript 到 JavaScript
- `prepublishOnly` - 自動編譯並設置權限
- 更新 `.gitignore` 排除 `dist/` 目錄

## [1.0.11] - 2026-01-20

### Added
- 將 `@google/genai` 添加到 dependencies

## [1.0.10] - 2026-01-20

### Changed
- 初始版本號調整

## [1.0.9] - 2026-01-20

### Added
- 初始發布版本
